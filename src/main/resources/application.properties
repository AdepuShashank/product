spring.application.name=Product
spring.datasource.url=jdbc:mysql://mysql:3306/userdb

spring.datasource.username=myuser
spring.datasource.password=mypass

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=true
server.port=8082
# to tables auto create do this
spring.jpa.hibernate.ddl-auto=update

# spring.data.redis.host=localhost
# spring.data.redis.port=6379

eureka.client.service-url.defaultZone=http://service-register:8761/eureka

eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
# eureka.client.service-url.defaultZone=http://eureka-service.default.svc.cluster.local/eureka/
# eureka.instance.prefer-ip-address=true
# eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}

# Prefer IP address (instead of hostname) for instance registration
# eureka.instance.prefer-ip-address=${EUREKA_INSTANCE_PREFER_IP_ADDRESS:true}